.row
  .span8.offset2
    %h2 Here you go:
    = text_area_tag 'secret_data',
                    @secret.data.chomp,
                    :class => 'span8 disabled_text input',
                    :disabled => true
    %hr
    %span.palette-paragraph
      This link will be gone if you refresh or close this tab
:javascript
  $(document).ready(function() {
      var clip = new ZeroClipboard($("#d_clip_button"))
  });
