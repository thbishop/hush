.row
  .col-sm-6.col-md-offset-3
    = form_for @secret, html: { class: 'new_secret form-horizontal' } do |f|
      -error_class = 'error' if @secret.errors.any?
      %div{ :class => "#{error_class}" }
        = f.text_area :data,
                      :rows => 8,
                      :placeholder => 'Put your secret here...',
                      :class => 'col-sm-8 form-control'
      %div.form-group
      %div
        = f.submit :class => 'btn btn-large btn-block form-control'
      %hr
      %span.palette-paragraph
        The link is only valid for 10 minutes or one visit (which ever comes first). After that it's deleted.
